# Адрес экспортёра Prometheus админки — по тем же причинам, что и у
# chillhub-api (см. deploy/systemd/chillhub-api.service.d/20-metrics.conf).
#
# Порт отдельный: у админки свой реестр, и продуктовые числа админки
# (публикации версий, техработы, обратная связь) не должны смешиваться с
# числами публичного API в одной цели Prometheus.
# ЗАВИСИМОСТЬ ОТ DOCKER. Адрес 172.17.0.1 принадлежит мосту docker0, и до
# старта докера его на хосте нет. Без этой зависимости служба стартовала
# одновременно с ним и не успевала: привязка к несуществующему адресу не
# проходила, процесс поднимался БЕЗ слушателя метрик и молчал об этом —
# ошибкой это не считается, Restart=on-failure базового юнита не срабатывает,
# и цель в Prometheus просто оставалась недоступной. Поймано 04.09.2026,
# когда выяснилось, что цель не собиралась с самой загрузки контейнера.
[Unit]
After=docker.service
Wants=docker.service

[Service]
Environment=ADMIN_METRICS_LISTEN_ADDR=172.17.0.1:55778
